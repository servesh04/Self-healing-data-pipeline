"""STUB — Phase 2. Real narrow-fix specialist (LLM, constrained to the
`casts` section of the mapping) arrives in Phase 3.

Deliberately a no-op: reaching this node — visible in the printed node
sequence — is what Phase 2 needs to prove, not any state change. propose_patch
is the stub that decides the hardcoded patch content, keyed by drift_class.
"""


def spec_type(state: dict) -> dict:
    return {}
